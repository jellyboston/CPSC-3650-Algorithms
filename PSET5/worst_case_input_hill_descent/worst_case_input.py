def make_grid(h, w):
    """ Returns an h-row, w-column 2-D array (list of lists) of numbers.

        h -- 9
        w -- 11
    """
    # hint: keep things linked up with all 2-digit integers
    # use +n or -n for single-digit values
    # values needn't be unique
    # YOUR SOLUTION HERE
    return [[0] * 11 for r in range(9)]
